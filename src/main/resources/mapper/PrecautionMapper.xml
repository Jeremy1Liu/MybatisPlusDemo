<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.mybatisplusdemo.mapper.PrecautionMapper">

    <resultMap id="BaseResultMap" type="com.atguigu.mybatisplusdemo.pojo.Precaution">
        <id property="id" column="id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,name
    </sql>

    <select id="selectByUserId" resultType="com.atguigu.mybatisplusdemo.pojo.Precaution">
        SELECT p.*
        FROM precaution p
                 JOIN user_precaution up ON p.id = up.precaution_id
        WHERE up.user_id = #{userId}
    </select>
    <select id="getPrecautionsByUserId"
            resultType="com.atguigu.mybatisplusdemo.pojo.Precaution">
        select *
        from precaution
        where id in
              (select precaution_id from user_precaution where user_id = #{userId})
    </select>
    <select id="getPrecautionByDiseaseId"
            resultType="com.atguigu.mybatisplusdemo.pojo.Precaution">
        select *
        from precaution
        where id in
              (select precaution_id from disease_precaution where disease_id = #{diseaseId})
    </select>
    <select id="getPrecautionByDiseaseName"
            resultType="com.atguigu.mybatisplusdemo.pojo.Precaution">
        select *
        from precaution
        where id in
              (select precaution_id
               from disease_precaution
               where disease_id in
                     (select id from disease where name = #{diseaseName}))
    </select>
</mapper>
